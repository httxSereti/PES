[project]
name = "pes-bot"
version = "1.0.1"
description = "Manage your EStim devices and sessions over the internet."
requires-python = "==3.12.6"
authors = [{ name = "graviky" }, { name = "Sereti" }]
dependencies = [
    "pyttsx3",
    "python-dotenv",
    "requests",
    "pybluez",
    "bleak",
    "pyserial",
    "poetryup",
    "kaleido==0.2.1",
    "playsound3>=3.2.3,<4",
    "mypy>=1.16.0,<2",
    "fastapi>=0.121.2,<0.122",
    "uvicorn>=0.38.0,<0.39",
    "websockets>=15.0.1,<16",
    "cuid2>=2.0.1",
    "pyjwt>=2.10.1",
    "aiosqlite>=0.22.1",
    "sqlalchemy>=2.0.49",
    "structlog>=25.5.0",
    "pydantic>=2.12,<3",
    "aiohttp>=3.14.3",
]

[tool.uv]
package = false

[tool.uv.sources]
pybluez = { git = "git+https://github.com/pybluez/pybluez.git" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.mypy]
check_untyped_defs = true
